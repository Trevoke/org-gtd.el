HIGH IMPACT -- and broader than the builder. Root cause is in org-gtd-view-show itself: re-invoking it while an *Org Agenda* buffer is already DISPLAYED does not rebuild the buffer. This affects the builder live-preview AND org-gtd-view-run (recall view A, then view B -> still shows A) AND any repeated org-gtd-view-show call.

Repro A (builder, org-agenda-sticky=nil): set a filter to value A (area=Work) -> narrows correctly. Change same filter to value B whose result includes items A hid (area=Home) -> preview keeps showing A. RET does not fix it. Badge/state DO update to B. Adding a filter (nil->value) and clearing one (value->nil) work; only value->value fails.

Repro B (plain eval, no builder): with a next-action agenda already showing, M-: (org-gtd-view-show '((name . "Habits") (type . habit))) still shows the next actions, not the habit. Confirmed during QA: habit filtering could not be exercised at all because the agenda would not re-render.

Evidence:
- Badge shows the new value (builder state correct).
- (with-current-buffer "*Org Agenda*" ...) still contains the OLD result after the change + RET -> the agenda BUFFER is not rebuilding. No stray agenda buffers.
- Headless repro of two consecutive org-gtd-view-show calls (Work then Home) rebuilds correctly (sticky=nil) -- so DSL/compile/skip-functions are fine. Failure is interactive-only, tied to an agenda buffer being displayed in a window.

Two compounding faults:
1. org-gtd-view-show -> (org-agenda nil "g") does not rebuild the displayed *Org Agenda* buffer on re-invocation (interactive). Likely org-agenda reusing the existing displayed buffer/window under the same dispatch key without a redo, and/or the window fight with the transient (sibling yak). Investigate forcing a rebuild (kill/erase the agenda buffer first, or org-agenda-redo, or a unique key per render).
2. org-gtd-view-manager--preview-now sets --preview-last after a render that returned without error even though the buffer did not rebuild; --preview-changed-p then reports 'unchanged' so RET (--preview) skips re-rendering -> stale view unrecoverable. Make explicit RET bypass the changed-p guard; only update --preview-last when a rebuild actually happened.

Blocks: habit/not-habit visual verification, area/effort value changes, and view-run recall of a second view. This is the linchpin bug of the walkthrough.

Source: manual QA walkthrough 2026-07-09, steps 3.3 / 3.4 / 4.2.

UPDATE (QA 2026-07-09): Narrowed. Standalone repeated org-gtd-view-show calls DO rebuild correctly interactively (evaled ((type . habit)) then ((type . habit)(not-habit . t)) back-to-back WITHOUT killing *Org Agenda* -> second showed empty as expected). So the fault is specific to the live-preview path (debounced idle-timer render while the transient owns the window layout), NOT org-gtd-view-show. Strongly implicates the transient/window interaction (see sibling k65z) and/or the --preview-last optimistic cache -- not agenda buffer reuse in general.