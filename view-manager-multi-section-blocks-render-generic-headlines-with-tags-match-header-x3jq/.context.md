In a multi-section (blocks) view, each block renders with org-agenda's generic default header 'Headlines with TAGS match: LEVEL>0+ORG_GTD=...' instead of a meaningful section label. Two next-action sections both show the identical generic header, so it looks like adding a section duplicated/overwrote the first.

ROOT CAUSE (verified headlessly): section specs carry no 'name', so org-gtd-view-lang--translate-to-native-block sets org-agenda-overriding-header to nil -> org-agenda falls back to the generic header. The filtering IS correct (blocks are distinct: Actions/NEXT vs Delegated/WAIT); only the header is missing. A flat single-section view is fine because its spec has the view name as the block header.

FIX: in org-gtd-view-manager--compile-view (multi/blocks branch) synthesize a per-block (name . <section badge>) so each block gets a real header ('next-action · Work' etc.). In --build-load, strip any name from each block when populating --build-sections (sections carry no name; it is re-synthesized on compile). Ensure --badge-section ignores a name key so the list-row 'N sections: ...' badge is unchanged.

Source: eneg manual re-QA 2026-07-10 (step 2). Verified fix: blocks WITH names render 'Next actions' / 'Delegated'.