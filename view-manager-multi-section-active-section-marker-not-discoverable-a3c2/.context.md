During eneg QA the tester couldn't find the section state: 'it doesnt say Section 1/1' and 'I have no idea what badge marker is supposed to be'. The info IS rendered -- --build-summary shows 'View: <name> - Section i/N [ b0 | ▸ b1 | b2 ]' in the transient's top description line, with ▸ marking the active section -- but users look in the agenda preview and/or don't notice/understand the summary line. Related to iv4b (summary not obviously a header).

Fix ideas: list the sections as ROWS in the 'Sections' transient group (like the list manager's rows, with ▸ on active and each section's badge) so section state is visible right where the section keys are; and/or make the Section i/N marker more prominent. Note: fixing the sibling 'generic block header' yak also makes sections visible/labeled in the preview itself, which helps a lot.

Source: eneg manual re-QA 2026-07-10 (steps 1/3).