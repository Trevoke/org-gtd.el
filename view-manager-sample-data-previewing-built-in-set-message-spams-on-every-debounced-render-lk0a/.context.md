When org-agenda-files is empty, org-gtd-view-manager--render-preview messages 'sample data - your agenda-files are empty - previewing org-gtds built-in set' on EVERY render. With the P1 fix making the debounced preview fire on every filter change, rapid edits spam *Messages*/echo area. Pre-existing, low severity. Fix idea: show the banner once per builder session (or only on the first sample render), not every render. Source: code review during P2 fixes, 2026-07-09.