SYMPTOM (PR #294 QA): running M-x org-gtd-review-schedule twice creates a second review
reminder habit; the "A review reminder already exists — schedule another?" y-or-n-p
does not fire on the 2nd run, so org-gtd-review--reminder-exists-p is returning nil
despite a habit existing.

HYPOTHESIS: org-gtd-review--reminder-exists-p (and --insert-reminder-body) scan
(org-gtd--default-file) = org-gtd-tasks.org. But the habit is created via
org-gtd-create-item (quote habit) -> org-gtd-process-heading, which REFILES the item to
the habit type target — possibly a different file than org-gtd--default-file. If so the
guard scans the wrong buffer and never sees the habit. Headless repro in progress to
confirm the write-target vs scan-target mismatch.

SEVERITY: real bug in NEW code (org-gtd-review.el), should fix before merging PR #294.
Not QA-blocking (duplicate habit is a minor annoyance, no data loss).
