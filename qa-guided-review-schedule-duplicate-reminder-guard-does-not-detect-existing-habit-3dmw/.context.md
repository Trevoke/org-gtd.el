STATUS (2026-07-08): DOWNGRADED from "confirmed bug" to UNVERIFIED — likely a setup
artifact, must be settled with a proper e-unit test, NOT raw batch eval.

What is established:
 - The guard LOGIC is correct: planting a well-formed habit (ORG_GTD=Habit, title=
   profile name) in org-gtd--default-file makes org-gtd-review--reminder-exists-p return
   t. Verified headless.
 - The habit refiles INTO org-gtd--default-file (org-gtd-refile.el:225), the same file the
   guard scans — so NO file mismatch.
 - The original duplicate was observed in an IMPROPERLY configured sandbox (legacy
   org-gtd-refile-to-any-target=t default + missing keyword/agenda setup). See
   [[qa-requires-proper-org-gtd-setup]].
 - Cannot reproduce the real creation via eldev emacs --batch: org-gtd-create-item
   (quote habit) is INTERACTIVE (hangs on a "Tags:" prompt from the organize/area-of-
   focus step). Raw batch eval is the wrong tool.

NEXT (the arbiter): write an e-unit test using ogt-eunit-with-mock-gtd +
with-simulated-input / cl-letf stubs (like existing review-schedule-test.el) that runs the
REAL org-gtd-review-schedule twice and asserts only ONE reminder habit exists and the 2nd
call hits the guard. GREEN => no bug (was an artifact), close this. RED => real bug and we
already have the regression test. Do NOT "fix" anything until this test tells us which.
