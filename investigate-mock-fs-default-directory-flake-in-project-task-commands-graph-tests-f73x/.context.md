RESOLVED 2026-07-08 (commits dbb90d7 + c211212 on worktree-checklists-guided-review-design / PR #294).

Root cause was THREE independent cross-test state leaks (not native-comp/trampolines):
1. org-id-get-create -> uuidgen subprocess -> chdir into virtual /mock:/gtd/ fails
   ("Setting current directory"). Fixed: advise org-id-new to use a real default-directory.
2. mock-fs handler is registered permanently; only mock-fs--current is scoped by
   with-mock-fs. A /mock:/tmp temp file gets persisted to the real .org-id-locations file
   and reloaded/file-truename'd by a later non-mock test -> gethash on nil mock-fs--current
   -> (wrong-type-argument hash-table-p nil). Fixed: ephemeral org-id-locations-file per run
   + reset org-id global scan state in accessors-test setup.
3. todo-filter-test lacked (require 'org-gtd-skip) and org-todo-keywords setup; passed only
   by inheriting leaked keyword config. Fixed: add require + setup-each.

Also enabled seed support in the runner (was the blocker to reproducing): eldev etest now
honors file/dir params + --seed=N; run-tests.sh reports/reproduces seeds. The etest command
had been IGNORING its file arg (single-file runs ran the whole suite).

Verified 1369/1369 across all documented repro seeds (1783402152928884, 1783391618627444,
1783391384945150, 1783401908999533, 1783400636610272) and 8 random seeds.
