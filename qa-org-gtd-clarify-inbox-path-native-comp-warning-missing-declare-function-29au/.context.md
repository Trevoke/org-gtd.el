DONE (2026-07-08, commit 2db8c2e): added (declare-function org-gtd-inbox-path
"org-gtd-capture") to org-gtd-clarify.el after the require block. Canonical fix for the
native-comp "not known to be defined" warning; a require would risk a load cycle. Compile
--warnings-as-errors clean. Note: the warning only surfaces under async native-comp (load
order), not in eldev batch, so it was not empirically A/B-reproducible here, but the fix is
the documented remedy and native-compiles cleanly.
