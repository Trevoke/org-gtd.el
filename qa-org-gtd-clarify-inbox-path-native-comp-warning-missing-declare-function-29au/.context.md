WARNING (native-compiler): org-gtd-clarify.el:696:22: the function org-gtd-inbox-path is
not known to be defined.

ROOT CAUSE: org-gtd-inbox-path is defined in org-gtd-capture.el:111 and called from
org-gtd-clarify--queue-save-to-inbox (org-gtd-clarify.el:696), but org-gtd-clarify.el
neither requires org-gtd-capture nor has a declare-function for it. Benign at runtime
(function exists once loaded); the byte/native compiler just cannot see it.

FIX: add (declare-function org-gtd-inbox-path "org-gtd-capture") near the top of
org-gtd-clarify.el. PRE-EXISTING (part of the duplicate-queue feature; PR #294 did not
touch clarify.el) so likely a SEPARATE tiny commit/PR, not bundled into #294.
