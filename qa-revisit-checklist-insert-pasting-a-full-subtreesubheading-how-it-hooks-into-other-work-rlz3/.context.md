org-gtd-checklist-insert copies the template as a FULL SUBTREE via org-paste-subtree at
target-level = (1+ (or (org-current-level) 0)) — i.e. the template heading (e.g.
"Weekly Review triggers") plus its "- [ ]" items becomes a demoted SUBHEADING under the
current heading. Open question from QA: is a nested heading the right shape, or should
insertion drop just the checkbox items (no wrapper heading) at point? Depends on how
checklists are meant to compose with projects/tasks/habits downstream.
ACTION: survey intended downstream uses (project templates? habit bodies? standalone?)
before locking the insertion shape. Related: [[qa-checklist-insert-adds-extra-newlines-
when-point-is-in-heading-body-insert-at-point-69n0]].
