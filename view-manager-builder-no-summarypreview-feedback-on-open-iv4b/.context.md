On opening the builder (c): the tester couldn't tell where the summary 'header' was ('What header? I don't know where that is supposed to be'), and the preview did not refresh on open -- a stale agenda from a previous action was still showing. The summary line only became noticeable ('now I do see untitled') after pressing RET.

Two sub-points:
1. The summary line (View: <name> - <badge>) is not visually distinct as a header; consider making it clearer / labeled.
2. The builder does not render a fresh preview on open (only on first RET or infix change), so if an agenda is already visible it looks like the builder is previewing stale data. Consider auto-rendering the preview once on open.

Source: manual QA walkthrough 2026-07-09, step 3.1-3.2.