In the builder transient, the five groups (Type / Time / Structural / Metadata / Prefix) stack in a single vertical column and use little of the transient's width. Tester noted twice it 'would be slightly nicer if they used more of the space' / 'use more of the horizontal space of the transient'.

Fix: lay the groups out in multiple columns (transient supports multi-column vectors) so the builder is wider and shorter.

Source: manual QA walkthrough 2026-07-09, step 3.1/3.2.

FIX IMPLEMENTED (commit 3a6ebed): builder groups arranged 3+2 into side-by-side columns (Type/Time/Structural on row 1, Metadata/Prefix on row 2) by wrapping group vectors into outer row vectors. Guard test asserts all 15 infix + 3 action keys stay bound. NEEDS MANUAL RE-QA: confirm the groups render side by side and fit the width, and all keys still work.