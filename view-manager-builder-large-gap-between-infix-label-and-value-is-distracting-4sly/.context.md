In the builder infix rows, there's a wide gap between the label and its value, e.g. 't type            next-action', 'w when              —'. Cause: org-gtd-view-manager--infix-description formats as '%-13s %s' (pads the label to 13 columns), and transient's column layout may add more. Reported as disruptive/distracting to read (QA 2026-07-10, screenshot).

Fix idea: reduce the pad width (e.g. %-11s or tighter), or right-align values per column, or drop the fixed pad so the value sits close to the label. Small formatting change in --infix-description; verify columns still align acceptably across the 3+2 layout.

Source: eneg re-QA 2026-07-10.