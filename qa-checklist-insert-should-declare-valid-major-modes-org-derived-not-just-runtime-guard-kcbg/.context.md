org-gtd-checklist-insert currently guards at runtime only:
  (unless (derived-mode-p (quote org-mode)) (user-error "Not in an org buffer"))
Suggestion: also declare valid modes so it is filtered out of M-x in non-org buffers,
via (declare (modes org-mode)) in the function body (Emacs 28+, honored when
read-extended-command-predicate = command-completion-default-include-p).
Keep the runtime guard as the hard fallback (declare only affects completion display).
Verify Emacs 28.1 baseline supports (declare (modes ...)).
