DONE (2026-07-08, commit 796e8f5): Added (declare (modes org-mode)) to
org-gtd-checklist-template-insert so M-x filters it out of non-org-derived buffers
(Emacs 28.1+ command-modes). Runtime derived-mode-p guard kept as the hard stop. Test:
checklist/insert-declared-for-org-mode asserts (command-modes ...) => (org-mode).
