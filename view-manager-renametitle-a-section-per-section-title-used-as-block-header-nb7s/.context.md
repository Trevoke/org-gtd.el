User request during eneg QA 2026-07-10: let the user give a section a custom title. The DSL already renders a block's 'name' as its agenda header (the x3jq fix uses this), so per-section titles are fully supported. Add a builder key (e.g. M-r) to set the ACTIVE section's title; store it per-section; --compile-view uses the section's title as the block name/header when set, else the synthesized badge (x3jq). --build-load must preserve a user-set title (distinguish from the synthesized name it currently strips). Pairs with the summary-display bug and a3c2 (same Sections-group area).