#+TITLE: org-gtd v5 Journal — feature decisions
#+STARTUP: overview
#+TODO: UNDECIDED(u) REVIEWING(r) TODO(t) | BUILD-V5(b) DEFER(f) DROP(x) DONE(d)

A working decision log for org-gtd v5. Every recommended-but-not-turnkey
feature (the 73 gaps from the feature analysis) gets one heading, and we go
through them *one at a time* to make a clean, coherent decision about each.

* How this journal works
:PROPERTIES:
:VISIBILITY: all
:END:

- *Source of truth*: =docs/feature-analysis/gaps/recommended-not-implemented.md=
  (122 recommended features; 49 implemented; 73 gaps). Per-feature UX designs,
  where they exist, live in =docs/feature-analysis/ux-workflows/REC-*.md=.
- *Process*: pick the next item from [[* Backlog — awaiting decision][Backlog]],
  discuss it, then file a full heading under [[* Decisions][Decisions]] in its
  GTD area with a decision keyword, and check it off the backlog.
- *Decision keywords* (the =#+TODO= line):
  - =UNDECIDED= — not yet reviewed.
  - =REVIEWING= — actively under discussion.
  - =TODO= — a real, committed to-do that's *tracked* (a yak), but whose exact
    release scope (v5 vs later) isn't fixed yet. Not shelved — on the list.
  - =BUILD-V5= — commit to building it for 5.0.0.
  - =DEFER= — real, but later (5.x or a future adapter layer); say where.
  - =DROP= — decided not to do; say why.
  - =DONE= — already implemented.
- *Per-heading properties*: =:REC_ID:=, =:STATUS:= (current implementation
  status: Implemented / Partial / Via-config / Not-implemented), =:STRENGTH:=
  (must / should / may), =:TYPE:= (tool / methodology), =:DISPOSITION:= (prior
  =V-xx= adjudication, if any — those are already-made calls, not to
  re-litigate), =:UX_DOC:= (whether a REC-* workflow design exists).
- *Tags*: strength + area, for filtering (e.g. =C-c \ +must+engage=).
- *Standing constraints* (do not propose against these): no org-ql /
  org-super-agenda dependency; no forced org-agenda; reference and contacts
  systems are future *adapter* layers, not bundled stores.

* Backlog — awaiting decision
:PROPERTIES:
:VISIBILITY: children
:END:

Ordered by strength, then GTD area. Check off as each is filed under Decisions.
Count: 3 must + 32 should + 36 may transcribed (gap scoreboard = 73; ~2
=may= stragglers to reconcile against the source as we go).

** MUST (3)
- [X] =REC-REF-01= → *DONE* — already implemented by =org-gtd-review-schedule=
- [X] =REC-ENG-02= → *DEFER* (documentation — Diataxis tutorial; see Engage)
- [X] =REC-ENG-03= → *BUILD-V5* (energy criterion + DSL keyword; wizard dropped; see Engage)

** SHOULD (32)
*** Reflect
- [X] =REC-REF-04= → *BUILD-V5* (ship a stock Recovery review profile + docs; see Reflect)
- [X] =REC-REF-05= → *BUILD-V5* (schedule per-horizon reviews like the weekly habit; see Reflect)
- [X] =REC-REF-06= → *BUILD-V5* (maintenance profile; horizon-events via REF-05; see Reflect)
- [X] =REC-REF-07= → *DEFER* (docs: coverage note in review + view-creation docs; see Reflect)
- [X] =REC-REF-08= → *DONE* (satisfied by REF-01 + REF-02)
- [X] =REC-REF-09= → *BUILD-V5* (time-block the weekly-review habit; prompt time, default 2h; see Reflect)
*** Engage
- [X] =REC-ENG-05= → *DEFER* (docs — folded into the four-criteria how-to; see Engage)
- [X] =REC-ENG-06= → *DEFER* (docs — explanation of the three work modes; see Engage)
*** Projects
- [X] =REC-PRJ-04= → *BUILD-V5* (subproject rollup; parallel/seq handled + yak filed; see Projects)
- [X] =REC-PRJ-06= → *DEFER* (implemented enough via clarify side windows; UI-review yak; see Projects)
- [X] =REC-PRJ-07= → *DROP* (planning checklist doesn't fit the flow; docs note + side-window yak; see Projects)
- [X] =REC-PRJ-08= → *DROP* (no structural distinction needed; docs note; see Projects)
- [ ] =REC-PRJ-10= /Not-impl/ — verb-starter checklist for naming projects
*** Clarify
- [X] =REC-CLA-06= → *TODO* (opt-in LLM semantic clarify assist; yak …-d009; see Clarify)
- [X] =REC-CLA-10= → *TODO* (same opt-in LLM clarify assist; yak …-d009; see Clarify)
*** Capture
- [X] =REC-CAP-06= → *BUILD-V5* (ad-hoc mind-sweep command + fuller trigger lists; horizons-prompts split to yak; see Capture)
*** Next Actions
- [X] =REC-NXT-05= → *DEFER* (docs — suggest a Read/Review someday/maybe category; see Next Actions)
*** Tickler
- [X] =REC-TIC-02= → *BUILD-V5* (recurrence via org-habit-ng; lead-time DROP→docs/DEADLINE; see Tickler)
*** Delegate
- [X] =REC-DEL-03= → *BUILD-V5* (design-gated: snapshot/restore seam needs its own design; see Delegate)
*** Agendas
- [X] =REC-AGE-01= → *DEFER* (docs howto; users may use a PKB/org-roam; see Agendas)
*** Reference
- [X] =REC-KNO-02= → *TODO* (GTD→PKM action surface; yak …-c5ql; see Reference)
- [X] =REC-KNO-04= → *TODO* (likely a PKM-tool no-op; yak …-c5ql; see Reference)
- [X] =REC-KNO-05= → *TODO* (likely a PKM-tool no-op; yak …-c5ql; see Reference)
*** Horizons
- [X] =REC-HOR-03= → *DEFER* (docs; review cadence = REF-05; see Horizons)
- [X] =REC-HOR-04= → *DEFER* (docs; definition = LLM-horizon yak; see Horizons)
- [X] =REC-HOR-05= → *DEFER* (docs; see Horizons)
- [X] =REC-HOR-08= → *DEFER* (docs explanation — top-down prioritization; see Horizons)
*** UI
- [X] =REC-UI-02= → *BUILD-V5* (command-center count annotations; dashboard-rework aspiration → UI-01; see UI / Views)
- [X] =REC-UI-04= → *BUILD-V5* (walk missed calendar → migrate; rides the generalized walk engine; see UI / Views)
- [X] =REC-UI-05= → *BUILD-V5* (chain-end closure prompt; covers under-defined project / PRJ-08 spillover; see UI / Views)
*** Cross-cutting
- [X] =REC-X-10= → *TODO* (unified, friendlier new-user UX; yak …-vm2c; see Cross-cutting)
- [X] =REC-X-14= → *DEFER* (docs howto — file sync (Nextcloud), mobile apps, inbox setup; see Cross-cutting)

** MAY (36)
*** Capture
- [X] =REC-CAP-08= → *DEFER* (docs — clock-out capture recipe; see Capture)
- [X] =REC-CAP-09= → *DEFER* (folded into CAP-06: template-agnostic mind-sweep + "current reality" template; see Capture)
*** Clarify
- [X] =REC-CLA-09= → *DROP* (docs note — 2-min is a guideline, no enforced cutoff; see Clarify)
- [X] =REC-CLA-11= → *DROP* (exits already available for every item via organize; see Clarify)
*** Projects
- [X] =REC-PRJ-09= → *DROP* (digital = already separated by state/query; see Projects)
- [X] =REC-PRJ-12= → *DEFER* (fold habit-spawn into UI-05; DAG-templates gap → yak …-vj3y; see Projects)
*** Next Actions
- [X] =REC-NXT-04= → *DROP* (folded as example into NXT-05 someday-categories docs; see Next Actions)
*** Delegate
- [X] =REC-DEL-04= → *DROP* (feature unneeded; research yak on delegation-UX impact …-1u47; see Delegate)
- [ ] =REC-DEL-05= /Not-impl/ — trackable-channel guidance (email > note > voice)
*** Reference
- [ ] =REC-KNO-03= /Not-impl/ — contacts as pure reference (V-09, non-goal — closed)
- [ ] =REC-KNO-06= /Not-impl/ — user-definable reference forms/templates + editor escape
*** Checklists
- [ ] =REC-CHK-02= /Via-config/ — recurring reflection-prompt lists at intervals
*** Agendas
- [ ] =REC-AGE-02= /Partial/ — aggregated per-person view + printable brief
- [ ] =REC-AGE-03= /Not-impl/ — meeting lifecycle object; outcomes to inbox
*** Reflect
- [ ] =REC-REF-10= /Partial/ — optional annual / year-end stock-taking (V-25)
*** Engage
- [ ] =REC-ENG-08= /Via-config/ — "weird time" micro-window short-action view
*** Horizons
- [ ] =REC-HOR-06= /Via-config/ — personal standards/values as decision reference (adjudicated #4 — closed)
- [ ] =REC-HOR-07= /Not-impl/ — unified cross-horizon "overview of my life" view (adjudicated #3, MAY)
*** UI
- [ ] =REC-UI-01= /Via-config/ — dashboard home view (lists + calendar)
- [ ] =REC-UI-03= /Partial/ — methodology-teaching coaching dialogs
- [ ] =REC-UI-06= /Partial/ — exactly-one-date semantics per action
- [ ] =REC-UI-07= /Not-impl/ — verb-first action entry and routing
- [ ] =REC-UI-08= /Via-config/ — customizable list sorting
- [ ] =REC-UI-09= /Via-config/ — global multi-key search
- [ ] =REC-UI-10= /Partial/ — closure loops at every exit
- [ ] =REC-UI-11= /Via-config/ — rules-based automation (satisfied by org-edna)
- [ ] =REC-UI-12= /Via-config/ — print/export first-class
- [ ] =REC-UI-13= /Via-config/ — retrospective calendar archive/log (satisfied)
- [ ] =REC-UI-14= /Via-config/ — gateway to external apps while processing
- [ ] =REC-UI-15= /Via-config/ — alarms/timers incl. optional 2-minute timer
- [ ] =REC-UI-16= /Partial/ — composite rollup views per area/horizon
- [ ] =REC-UI-17= /Partial/ — project ledger & metadata
- [ ] =REC-UI-18= /Via-config/ — layered calendar (month/week/day)
*** Cross-cutting
- [X] =REC-X-04= → *BUILD-V5* (resolved with ENG-03 — the energy attribute; see Cross-cutting)
- [ ] =REC-X-09= /Partial/ — named project sub-lists, event-date sorted (V-13 — closed)
- [ ] =REC-X-15= /Not-impl/ — ≥50-actions completeness heuristic diagnostic

* Foundations
:PROPERTIES:
:VISIBILITY: children
:END:

Cross-cutting builds that multiple feature decisions depend on. Build these
first; they unlock clusters.

** BUILD-V5 (foundational) — generalize the item-walk engine            :foundational:
:PROPERTIES:
:STATUS:      partially built (someday-review is a coupled instance)
:DISPOSITION: BUILD-V5 (design-gated)
:DECIDED:     2026-07-15
:END:
The item-by-item *walk* engine already exists — =org-gtd-someday-review= has the
whole thing: a session with =:queue= of org-ids + =:position=, =--advance= to the
next item, a review major-mode with =d=/=c=/=q= keys, and LOGBOOK stamping. It's
just *coupled to someday/maybe*: =--find-items= hardcodes =ORG_GTD == someday=
and the actions are defer/clarify.

*Decision*: BUILD-V5 — *generalize* it into a reusable item-walk: parameterize
(a) which items it finds and (b) which actions apply per walk. This is the
design-§8 "=walk= step type" — but a refactor-and-reuse, not net-new. Needs a
short design for the generalization seam (find-fn + action-set + optional review
step integration).

*Unlocks* (consumers): [[*BUILD-V5 REC-UI-04 — consent-based migrate overdue calendar → next-action][REC-UI-04]] (walk missed calendar → migrate), the guided
review's walk-over-headings (projects / missed items), and the =may= items
REC-X-15 (completeness walk) and REC-CAP-09 (themed sweep) if built.

* Decisions
:PROPERTIES:
:VISIBILITY: children
:END:

Filed decisions, grouped by GTD area. Empty areas fill in as we go.

** Capture
*** DEFER REC-CAP-09 — "what's true right now?" themed current-reality sweep (folded into CAP-06) :may:capture:
:PROPERTIES:
:REC_ID:      REC-CAP-09
:STATUS:      Not-implemented → folded into CAP-06
:STRENGTH:    may
:TYPE:        methodology
:DISPOSITION: DEFER → folded into CAP-06 (template-agnostic sweep + stock template)
:UX_DOC:      REC-CAP-09.md
:DECIDED:     2026-07-15
:END:
Corpus-checked: this is *pure capture* (not reconciliation against the system) —
a themed trigger list of present-tense prompts ("what projects are active right
now?") that dumps to the inbox. Mechanically identical to the mind-sweep; only
the wording differs.

*Decision*: DEFER → folded into [[*BUILD-V5 REC-CAP-06 — guided mind-sweep / bulk-capture via trigger lists][REC-CAP-06]]: make =org-gtd-mind-sweep=
template-agnostic and seed a stock "What's true right now?" present-tense
template. No separate feature. (The corpus's stats-block/completeness console
assumes deferred features; not needed for the core sweep.)

*** DEFER REC-CAP-08 — capture hooks at activity boundaries             :may:capture:
:PROPERTIES:
:REC_ID:      REC-CAP-08
:STATUS:      Not-implemented → docs recipe
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DEFER → docs (clock-out capture recipe)
:DECIDED:     2026-07-15
:END:
An "anything to capture?" prompt at activity boundaries (after a call/meeting,
at clock-out). A few lines of user config, and baking it in risks nagginess.
*Decision*: DEFER → docs — a recipe showing =(add-hook 'org-clock-out-hook ...)=
to offer capture-to-inbox when you clock out.

*** BUILD-V5 REC-CAP-06 — guided mind-sweep / bulk-capture via trigger lists :should:capture:
:PROPERTIES:
:REC_ID:      REC-CAP-06
:STATUS:      Partial → building ad-hoc entry point + fuller lists
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (mind-sweep command); horizons-prompts split off
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Already shipped (CHK-01): starter "Weekly Review triggers" + "Mind sweep
prompts" templates, and a Weekly-Review "Mind sweep" step that walks a trigger
list with press-=c=-to-capture. Missing: a *standalone* ad-hoc entry point.

*Decision*: BUILD-V5 —
- A standalone =org-gtd-mind-sweep= command that walks the mind-sweep checklist
  with capture, runnable anytime (reuses the review's walk-and-capture flow).
  Make it *template-agnostic* — it can walk *any* trigger list — so [[*DEFER REC-CAP-09 — "what's true right now?" themed current-reality sweep (folded into CAP-06)][REC-CAP-09]]
  folds in as just "point the sweep at another template."
- Flesh out the seeded mind-sweep template into fuller *personal* and
  *professional* trigger lists, structured after GTD's incompletion-triggers
  appendix. *Copyright caveat*: write our own phrasing — do NOT copy Allen's
  appendix verbatim into the shipped package (the source texts are gitignored
  for exactly this reason).

*Split off — NOT part of CAP-06*: a "Horizons prompts" trigger template is
deliberately excluded. Pushing higher-altitude thinking on GTD newcomers is a
trap. Instead → a separate future feature: *LLM-assisted, iterative* horizon
definition (good-enough round, then refine). Tracked as yak
=llm-assisted-iterative-definition-of-ones-horizons-...-voqa=. Revisit alongside
REC-HOR-08 (top-down prioritization) when we reach Horizons.

** Clarify
*** DROP REC-CLA-09 — configurable do-it-now (2-min) threshold          :may:clarify:
:PROPERTIES:
:REC_ID:      REC-CLA-09
:STATUS:      Via-config → dropped (non-feature)
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DROP → docs note
:DECIDED:     2026-07-15
:END:
=org-gtd-quick-action= enforces no cutoff — the "2 minutes" is only guidance
wording; nothing to configure. *Decision*: DROP. Docs note: the 2-minute rule
is a rule of thumb (Allen says 2–5 min); apply your own judgment.

*** DROP REC-CLA-11 — three honest exits from a broken agreement        :may:clarify:
:PROPERTIES:
:REC_ID:      REC-CLA-11
:STATUS:      Partial → dropped (already available)
:STRENGTH:    may
:TYPE:        methodology
:DISPOSITION: DROP
:DECIDED:     2026-07-15
:END:
The "honest exits" (re-clarify/renegotiate, do, drop, defer) are the standard
=org-gtd-organize= choices already available on *every* item in the system.
Nothing to add. (Caveat: no "renegotiation" where there was no negotiation — but
the point stands.) *Decision*: DROP.

*** TODO REC-CLA-06 — extract hidden projects/actions from fuzzy items  :should:clarify:
:PROPERTIES:
:REC_ID:      REC-CLA-06
:STATUS:      Partial → tracked as an opt-in LLM-assist to-do
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: TODO — opt-in LLM semantic clarify assist (yak …-d009)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
"Is this fuzzy item actually several commitments?" is a *semantic* judgment, not
a mechanical split. A one-keystroke "split off to inbox" affordance would help
capture the extras, but recognising the hidden commitments is the hard part —
that's an *optional, opt-in LLM interface* doing semantic work on items during
clarification (only if the user wants it).

*Decision*: TODO — this is a real, committed direction, *not* shelved. Tracked
as yak
=opt-in-llm-semantic-assist-for-clarify-extract-hidden-projectsactions-cla-06-concreteness-check-to-keep-items-from-degrading-to-stuff-cla-10-d009=.
It needs its own design/brainstorm (surface, provider-agnostic, privacy,
off-by-default); v5-vs-later scope decided there. Shares the LLM-assist surface
with [[*TODO REC-CLA-10 — concreteness lint (keep items from degrading to "stuff")][REC-CLA-10]].

*** TODO REC-CLA-10 — concreteness lint (keep items from degrading to "stuff") :should:clarify:
:PROPERTIES:
:REC_ID:      REC-CLA-10
:STATUS:      Not-implemented → tracked as an opt-in LLM-assist to-do
:STRENGTH:    should
:TYPE:        methodology
:DISPOSITION: TODO — same opt-in LLM clarify assist (yak …-d009)
:UX_DOC:      REC-CLA-10.md
:DECIDED:     2026-07-15
:END:
Catching items that are still vague/non-actionable ("website", "Mom") and
nudging a rephrase into a concrete verb-first action. A *mechanical* verb-first
lint is easy but easy to make naggy (false positives); genuine concreteness
judgment is *semantic* — the same quality of work as [[*TODO REC-CLA-06 — extract hidden projects/actions from fuzzy items][REC-CLA-06]].

*Decision*: TODO — folded into the same opt-in LLM semantic clarify-assist yak.
Not a standalone mechanical lint (org-gtd stays non-preachy). Note the verb-first
theme also touches PRJ-10 and UI-07.

** Projects
*** DEFER REC-PRJ-12 — project spawning a recurring Habit on completion :may:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-12
:STATUS:      Partial → fold habit-spawn into UI-05
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DEFER → fold into UI-05; surfaced a DAG-templates gap (yak)
:DECIDED:     2026-07-15
:END:
Habits are first-class; the missing piece is an automated project→habit flow.

*Decision*: DEFER → fold the *habit-spawn* case into the [[*BUILD-V5 REC-UI-05 — mark-done closure prompt ("what's next?")][UI-05 closure prompt]]
as a "make this recurring (spawn a habit)" branch, built on org-habit-ng
(TIC-02). No standalone feature.

*Surfaced gap* (separate, tracked): the "spawn a *templated project*" case runs
into =org-gtd-clarify-project-templates= (=org-gtd-clarify.el:48=), which only
produces *sequential* projects — it can't encode/instantiate *DAG* projects
(parallel + blockers). Real definition + implementation work: a template format
capturing dependencies + blocker wiring on insert. Tracked as yak
=project-templates-should-handle-dag-projects-...-vj3y=; relates to
[[*BUILD-V5 REC-PRJ-04 — subproject rollup (+ parallel/sequential)][REC-PRJ-04]].

*** DROP REC-PRJ-09 — per-project Active vs Archive compartments        :may:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-09
:STATUS:      Via-config → dropped
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DROP
:DECIDED:     2026-07-15
:END:
The physical "active vs archive folder" compartment is moot digitally —
everything is already separated by state and query (active items under the
project, done/canceled archived via =org-gtd-archive=). *Decision*: DROP.

*** BUILD-V5 REC-PRJ-04 — subproject rollup (+ parallel/sequential)    :should:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-04
:STATUS:      Partial → sequencing handled; building subproject rollup
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (subproject rollup); parallel/seq = handled + yak
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Two halves, decided separately:

- *Parallel/sequential wiring* — *handled*. New projects get default sequential
  edna deps (=org-gtd-projects--add-default-sequential-dependencies=), and
  arbitrary DAGs are wired via blockers (=org-gtd-project-add-blocker= + the
  graph transient; =ORG_GTD_BLOCKS=/=ORG_GTD_DEPENDS_ON=). A *better UX* for
  this is worth doing but is tracked separately as a yak:
  =better-ux-for-parallelsequential-task-wiring-in-projects-blockersgraph-transient-v7i5=.

- *Subproject rollup* — *BUILD-V5*. Support nested projects whose completion
  rolls up to the parent. This is a genuine architectural change (project model,
  state machine, stuck detection, graph, agenda) and org-gtd has historically
  kept projects flat — so it needs its *own dedicated design/brainstorm* before
  implementation, not a quick add.

*** DEFER REC-PRJ-06 — Natural Planning Model scaffold                  :should:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-06
:STATUS:      Not-implemented → judged "implemented enough"
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER (implemented enough) + UI-review yak
:UX_DOC:      REC-PRJ-06.md
:DECIDED:     2026-07-15
:END:
The Natural Planning Model (purpose → outcome → brainstorm → organize → next
actions) is supported *enough* by two optional clarify side windows:
- =org-gtd-clarify-show-horizons= (=org-gtd-clarify.el:57=) — shows the horizons
  file (purpose/vision/areas) beside the item while clarifying.
- =org-gtd-clarify-display-helper-buffer= (=:67=) — live task-relationship view
  for the organize/wiring phase.

Key insight (user): in practice people don't *write* purpose/principles while
clarifying a project — they should already have them as higher horizons, and
just *reference* them. So the top NPM phases are look-up, not authoring, and
little real new work is needed here.

*Decision*: DEFER — no new scaffold. A UI pass over these side windows through
the NPM lens might improve them; tracked as yak
=ui-review-of-clarify-side-windows-horizons-dependency-helper-through-the-natural-planning-model-lens-prj-06-y5kl=.
The brainstorm-triggers side-window idea from [[*DROP REC-PRJ-07 — planning step as a next action + trigger list][REC-PRJ-07]] folds into this same review.

*** DROP REC-PRJ-07 — planning step as a next action + trigger list    :should:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-07
:STATUS:      Partial → dropped (workflow mismatch)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DROP + docs note; side-window idea → PRJ-06 yak
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Workflow analysis killed the "ship a planning checklist" idea. Project tasks are
*typed as outline headings* in the clarify WIP buffer (=org-gtd-organize= reads
them) and added later via =org-gtd-project-operations=. A trigger-list checklist
is a separate prompt buffer with *no bridge* from "checked item" to "project
task" — walk it, discard most, and you still go type the tasks. A trigger list
is a thinking *aid* (like the horizons window), not a task source.

*Decision*: DROP the checklist. PRJ-07's value is already covered:
- adding tasks → type them in the WIP buffer (+ project-operations later);
- "planning as a next action" → when you can't plan yet, type one task
  "Brainstorm/plan project X" — nothing to build;
- brainstorming aid → a "triggers" side window, if wanted, is folded into the
  PRJ-06 clarification-UI / side-window review yak.

*Docs follow-up*: note the planning-as-a-next-action pattern in the projects docs.

*** DROP REC-PRJ-08 — "look into" / commitment-to-decide (R&D) projects :should:projects:
:PROPERTIES:
:REC_ID:      REC-PRJ-08
:STATUS:      Partial → dropped (no distinction needed)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DROP + docs note
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
A "look into X" commitment is just a normal project whose *outcome is a decision*
("Decide whether to X" with research/compare/decide tasks), or a Someday/Maybe
item if not yet committed. Both are covered by existing types; a dedicated "R&D
project" type adds a distinction with no behavioral difference — and we
deliberately dropped =define-type=. Even the gap doc hedged "arguably fine."

*Decision*: DROP. Docs note: model "look into" work as an ordinary project with
a decision outcome, or incubate in Someday/Maybe until committed.

*Surfaced a bigger concern* (parked on [[*REC-UI-05]]): today a project whose
tasks are all DONE reads as complete/archivable, but sometimes it's merely
*under-defined* — the outcome isn't reached, it just needs new actions. The
closure prompt (REC-UI-05) is where "all-tasks-done ≠ complete; add more
actions?" should be handled instead of silent completion. Flagged on UI-05's
backlog entry.

** Next Actions
*** DROP REC-NXT-04 — optional informal "if I have time" list           :may:next:
:PROPERTIES:
:REC_ID:      REC-NXT-04
:STATUS:      Via-config → dropped (nothing structural)
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DROP → example in the NXT-05 docs
:DECIDED:     2026-07-15
:END:
Gap doc says "Missing: nothing structural" — an "if I have time" list is just a
named someday category or a tag+view. *Decision*: DROP; include it as a second
example ("If I have time") in the [[*DEFER REC-NXT-05 — Read/Review queue distinct from Reference][NXT-05]] named-someday-categories how-to.

*** DEFER REC-NXT-05 — Read/Review queue distinct from Reference        :should:next:
:PROPERTIES:
:REC_ID:      REC-NXT-05
:STATUS:      Not-implemented → satisfied via named someday lists (docs)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs (suggest a Read/Review someday/maybe category)
:UX_DOC:      REC-NXT-05.md
:DECIDED:     2026-07-15
:END:
org-gtd already supports named Someday/Maybe lists
(=org-gtd-someday-list-names=, =org-gtd-someday.el:42=) with grouped review, so
a "Read/Review" queue needs no code.

*Decision*: DEFER → docs. A Diataxis how-to / example that *suggests creating a
"Read/Review" Someday/Maybe category* (a named someday list) for committed
reading you'll do opportunistically. No stock code — just the recommended
pattern.

** Engage
*** BUILD-V5 REC-ENG-03 — four-criteria choice (context/time/energy/priority)   :must:engage:
:PROPERTIES:
:REC_ID:      REC-ENG-03
:STATUS:      Partial → building energy; 3/4 criteria already in DSL
:STRENGTH:    must
:TYPE:        methodology
:DISPOSITION: BUILD-V5 (carries REC-X-04); guided wizard dropped
:UX_DOC:      (none)
:CARRIES:     REC-X-04
:DECIDED:     2026-07-15
:END:
The DSL already filters/sorts on context (=tags=/=area-of-focus=/=who=), time
(=effort=) and =priority= (=org-gtd-view-language.el:181=) — three of Allen's
four criteria. Missing: *energy* (zero refs in source) and any guided ordering.

*Decision*: BUILD-V5 the **energy criterion**, which also resolves REC-X-04:
- Optional =ENERGY= property, *off by default* (no forced taxonomy).
- New =energy= filter keyword in the view DSL, parallel to =effort=/=priority=
  (predicate + keyword-list entry + view-builder infix), exposed in engage.
- *Drop* the four-step guided wizard — cuts against org-gtd's simplicity /
  no-forced-flow ethos; the four criteria are usable as composable filters +
  =org-gtd-engage-tagged=.

Payoffs beyond the feature: rounds out the base view DSL to the full
four-criteria set, and lets the docs map cleanly onto GTD's own
context/time/energy/priority language (streamlining).

*Docs follow-up*: a Diataxis how-to — "How to use GTD's four criteria
(context / time / energy / priority)" — anchored on this energy feature and
folding in the effort/time filtering ([[*DEFER REC-ENG-05 — filter by time available via Effort][REC-ENG-05]]) and the context recipe (ENG-02).
One home for all four criteria.

*** DEFER REC-ENG-05 — filter by time available via Effort             :should:engage:
:PROPERTIES:
:REC_ID:      REC-ENG-05
:STATUS:      Partial → DSL-composable; docs only
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs (four-criteria how-to)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
The "time" criterion. The DSL already filters on =effort=
(=org-gtd-view-language.el:181=), so a "what fits in my 10 minutes" view is
composable in the builder today. No separate turnkey command.

*Decision*: DEFER → docs. Fold into the four-criteria how-to (see [[*BUILD-V5 REC-ENG-03 — four-criteria choice (context/time/energy/priority)][REC-ENG-03]]) —
show building an effort-filtered "time available" view alongside energy,
context, and priority. (Same optional-metadata caveat as energy: only works if
the user sets Effort estimates.)

*** DEFER REC-ENG-02 — calendar-first, then context-filtered engage lists   :must:engage:
:PROPERTIES:
:REC_ID:      REC-ENG-02
:STATUS:      Partial → satisfied (calendar-first) + Via-config (context folding)
:STRENGTH:    must
:TYPE:        tool
:DISPOSITION: DEFER → documentation (Diataxis tutorial)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Calendar-first is already done — =org-gtd-engage-view-spec= (=org-gtd-engage.el:47=)
leads with a =calendar-day= block, then tickler/delegated/all-next-actions.
The only "missing" piece is folding the next-actions block by context, and that
is deliberately *not* built:

- The engage view is a *starter*. There's no telling how many contexts/tags a
  user has, so replacing "all next actions" with "all actions grouped by
  context" just swaps one blob for another — no real gain, and it would force a
  context taxonomy org-gtd doesn't (and shouldn't) impose.
- Per-context filtering already exists on demand (=org-gtd-engage-tagged=), and
  the shipped view builder lets a user compose and save their own
  context-folded engage view.

*Decision*: no code. Write a Diataxis-style *tutorial* showing how to build a
context-folded engage view with the view builder (calendar block + one section
per context). Follow-up doc task, not a code task.

*** DEFER REC-ENG-06 — three work modes as legitimate choices          :should:engage:
:PROPERTIES:
:REC_ID:      REC-ENG-06
:STATUS:      Partial → supported; framing only
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs (explanation); honors V-03
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Allen's three-fold nature of work: doing predefined work (lists), doing work as
it shows up (ad-hoc), and defining your work (capture + clarify/process).
org-gtd already serves all three (engage / capture / process). The gap is pure
conceptual framing.

*Decision*: DEFER → docs. A Diataxis *explanation* naming the three modes and
which commands serve each — a short section in the engage/overview docs.

** Reflect
*** BUILD-V5 REC-REF-05 — review cadence scaling per horizon           :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-05
:STATUS:      Via-config → building (supersedes V-21)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 — overrides prior V-21 (was "via-config")
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Overriding the prior V-21 "via-config" call. Rationale (user): same pattern as
the weekly-review habit (=org-gtd-review-schedule=) — easy, cheap, convenient,
and it *seeds real structure* into the user's files.

*Decision*: BUILD-V5 — ship stock higher-horizon review profiles (e.g. a
monthly area/goals review, an annual vision review) that are schedulable as
recurring habits at horizon-appropriate cadences, reusing the existing
=org-gtd-review-schedule= mechanism. Builds directly on the profile +
schedule infrastructure already in =org-gtd-review.el=.
- Note the overlap with [[*REC-REF-06]] ("horizon calendar events") — design them together.

*** BUILD-V5 REC-REF-06 — system-maintenance review                   :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-06
:STATUS:      Not-implemented → building maintenance profile
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (adjudicated #2 IMPLEMENT)
:UX_DOC:      REC-REF-06.md
:DECIDED:     2026-07-15
:END:
Two halves: (1) *horizon calendar events* — already covered by [[*BUILD-V5 REC-REF-05 — review cadence scaling per horizon][REC-REF-05]]; (2) a *system-maintenance review* aimed at the system's own health
(orphaned tasks, stuck projects, mis-categorised items). Not built as a flow,
but the pieces exist (stuck-projects view; orphan detection in
=org-gtd-graph-data.el= / =org-gtd-clarify.el=).

*Decision*: BUILD-V5 — ship a stock **"System Maintenance"** review profile that
walks the orphan/stuck/integrity views. Another member of the stock-profile
family (Weekly, Recovery, Horizon, Maintenance) on the one review engine.

*** BUILD-V5 (emergent) — seed-all review habits command              :emergent:reflect:
:PROPERTIES:
:REC_ID:      (emergent — not from corpus)
:STATUS:      new
:STRENGTH:    n/a
:TYPE:        tool
:DISPOSITION: BUILD-V5
:DECIDED:     2026-07-15
:END:
Emergent from the REF-01/04/05/06 cluster: a single command (e.g.
=org-gtd-review-schedule-all=) that installs the recommended family of
*scheduled* review habits in one go — Weekly (=.+1w=), horizon cadences
(areas/goals monthly, vision quarterly/annually), System Maintenance (monthly)
— at sensible default cadences, seeding real structure into the user's files.
Reuses =org-gtd-review-schedule='s duplicate-guard so re-running is safe.
- *Recovery is excluded*: it's an ad-hoc trigger (run when you fall off), not a
  scheduled habit.

*** BUILD-V5 REC-REF-04 — review on three triggers incl. recovery     :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-04
:STATUS:      Partial → shipping stock recovery profile
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (stock profile) + docs
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
The recovery *view* already exists (=org-gtd-reflect-missed-engagements=,
=org-gtd-reflect.el:255=). The guided review runs on named *profiles* in
=org-gtd-review-profiles= (=(PROFILE-NAME (PHASE-NAME step…)…)=; step types
prompt/command/view/checklist), so a guided recovery is a profile, not an engine.

*Decision*: BUILD-V5 — ship a stock **"Recovery"** review profile out of the box
(a short Get-Current-style walkthrough over the missed-items / waiting-for /
stuck-projects views) so get-back-on-track is turnkey, not user-assembled.
- Note: a second default profile means =org-gtd-review= now prompts for a
  profile at start (it already handles the multi-profile case).

*Docs follow-up*: document the *three kinds of review* — regular (weekly),
ad-hoc, and recovery — and which command/profile serves each.

*** DONE REC-REF-08 — lists kept totally current via the Weekly Review :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-08
:STATUS:      Implemented (was Partial only because the review didn't exist yet)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: satisfied by REF-01 + REF-02
:DECIDED:     2026-07-15
:END:
The gap's "what's missing" was literally "the review itself" — which now ships
(guided Weekly Review REF-02 + scheduling REF-01). Keeping lists current is the
outcome of running that review. Nothing left to build.

*** BUILD-V5 REC-REF-09 — time-block the Weekly Review                 :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-09
:STATUS:      Partial → building (scoped to the review itself)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (source-checked)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Source check (GTD Book 1 vs *Making It All Work*): Allen *forbids*
pre-scheduling routine discretionary next actions (calendar = hard landscape),
but Book 2 *does* endorse blocking calendar time for big-window important
actions during "weekly review mode" ("block out two hours for yourself in the
coming week"). So time-blocking the *action list* is out — but the Weekly
Review is itself exactly that kind of big-window action.

*Decision*: BUILD-V5 — tweak the weekly-review scheduling
(=org-gtd-review-schedule=, see [[*DONE REC-REF-01 — Weekly Review as a scheduled recurring event][REC-REF-01]]) so the created habit is a real *time
block*, not just a reminder: prompt for a start time, **default a 2-hour
duration** (editable later), producing a timestamped range like
=<2026-07-20 Mon 09:00-11:00 .+1w>=. This makes the review a genuine
hard-landscape appointment — faithful to Book 2, and it keeps the anti-pattern
(pre-scheduling ordinary actions) off the table. Do *not* generalise
time-blocking to the next-action list.

*** DEFER REC-REF-07 — all reminder locations reviewed equally         :should:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-07
:STATUS:      Partial → satisfied by REF-02
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Equal coverage is exactly what the guided Weekly Review profile enforces — its
steps walk every location (missed/calendar, waiting-for, next actions, stuck
projects, someday). The step list *is* the coverage guarantee; no separate
feature.

*Decision*: no code. Docs note — in two places:
- review docs: your review profile *is* your coverage guarantee; customizing it
  changes what gets swept.
- the view-creation / View Manager docs: a sentence reminding users that the
  views they build (and put in a review profile) determine coverage.

*** DONE REC-REF-01 — Weekly Review as a scheduled recurring event   :must:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-01
:STATUS:      Implemented
:STRENGTH:    must
:TYPE:        tool
:DISPOSITION: gap doc said Partial; superseded by shipped command
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Already done: =org-gtd-review-schedule= (=org-gtd-review.el:623=, autoloaded,
interactive) creates a *recurring habit* for a review profile — prompts for
profile / first date / org repeater (=.+1w= default), guards against duplicate
reminders (=org-gtd-review--reminder-exists-p=), inserts a body line, and the
habit surfaces in the engage view. Completion/streak tracking comes from
org-habit (repeater + LOGBOOK). The guided review's =--finish= advertises it as
a one-time tip. The gap analysis predates this command. No further work for v5.

*** DONE REC-REF-02 — guided Weekly Review                          :must:reflect:
:PROPERTIES:
:REC_ID:      REC-REF-02
:STATUS:      Implemented
:STRENGTH:    must
:TYPE:        tool
:DISPOSITION: adjudicated — implemented in the checklists + guided-review work
:UX_DOC:      REC-REF-02.md
:DECIDED:     2026-07-15
:END:
Landed on =develop= (former PR #294). Guided, step-through Weekly Review.
Deferred pieces — a session =walk= step over headings, and a review stats
block — are tracked in =docs/plans/2026-07-06-checklists-and-guided-review-design.md=
§8; several gated Cluster-A/B backlog items depend on them.
** Horizons
*Cluster*: all four DEFER → docs. Every build-shaped piece lives elsewhere —
review cadence = [[*BUILD-V5 REC-REF-05 — review cadence scaling per horizon][REC-REF-05]]; horizon *definition* = the LLM-horizon yak
(=llm-assisted-iterative-definition-of-ones-horizons-...-voqa=); project linkage
= =org-gtd-areas-of-focus= + config (adjudicated #5). What's left is docs. The
=horizons.org= file (template: Purpose/principles, Vision, Goals, Areas) is the
content home.

*** DEFER REC-HOR-03 — Goals/Objectives (30k), reviewed monthly–quarterly :should:horizons:
:PROPERTIES:
:REC_ID: REC-HOR-03
:STATUS: Via-config → docs
:STRENGTH: should
:DISPOSITION: DEFER → docs (review = REF-05; linkage = areas-of-focus, #5)
:DECIDED: 2026-07-15
:END:
Docs: keep goals in =horizons.org=; review them via a per-horizon review profile
(REF-05); link work through areas-of-focus.

*** DEFER REC-HOR-04 — Vision (40k), reverse-engineering + resurfacing  :should:horizons:
:PROPERTIES:
:REC_ID: REC-HOR-04
:STATUS: Via-config → docs
:STRENGTH: should
:DISPOSITION: DEFER → docs (definition via LLM-horizon yak; resurfacing via review)
:DECIDED: 2026-07-15
:END:
Docs: articulate vision in =horizons.org= (the LLM-horizon feature will help
define/refine it iteratively); resurface it in the horizon review.

*** DEFER REC-HOR-05 — Purpose & Principles (50k) as priority criterion :should:horizons:
:PROPERTIES:
:REC_ID: REC-HOR-05
:STATUS: Via-config → docs
:STRENGTH: should
:DISPOSITION: DEFER → docs
:DECIDED: 2026-07-15
:END:
Docs: purpose/principles live in =horizons.org= and inform decisions — the
top-down prioritization lens (HOR-08), not a coded filter.

*** DEFER REC-HOR-08 — top-down prioritization (vs ABC coding)         :should:horizons:
:PROPERTIES:
:REC_ID: REC-HOR-08
:STATUS: Partial → docs explanation
:STRENGTH: should
:DISPOSITION: DEFER → docs (explanation)
:DECIDED: 2026-07-15
:END:
Nothing buildable — top-down prioritization is a *mindset*: prioritize from your
horizons rather than ABC letter-codes. A Diataxis *explanation* piece.

** Reference
*Cluster note*: reference storage/browsing is a future *adapter* (denote /
org-roam / org-brain), not a bundled org-gtd store. But rather than DEFER these
vaguely, they're live TODOs to define the *action surface*: what GTD→PKM actions
org-gtd should own vs. what belongs to the PKM tool. All three tracked as yak
=reference-adapter-define-...-c5ql=.

*** TODO REC-KNO-02 — <60s filing; A–Z index; yearly purge              :should:reference:
:PROPERTIES:
:REC_ID:      REC-KNO-02
:STATUS:      Partial → TODO (define GTD→PKM action surface)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: TODO — reference-adapter action surface (yak …-c5ql)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
The *actionable* one: is there a useful set of "actions we take from GTD toward a
knowledge base" (fast-file a reference, hand off to the PKM, trigger a purge)
that org-gtd should build specific logic around? Figure that out; build the
GTD-side verbs, leave storage/index to the PKM.

*** TODO REC-KNO-04 — zero-friction new reference category while filing :should:reference:
:PROPERTIES:
:REC_ID:      REC-KNO-04
:STATUS:      Not-implemented → TODO (likely PKM-tool no-op)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: TODO — probably no-op for us (yak …-c5ql)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Creating a new reference *category* on the fly is very likely the PKM tool's
responsibility (denote/org-roam already do this), so probably a *no-op* for
org-gtd. Confirm during the action-surface investigation.

*** TODO REC-KNO-05 — browsable categorized reference index            :should:reference:
:PROPERTIES:
:REC_ID:      REC-KNO-05
:STATUS:      Not-implemented → TODO (likely PKM-tool no-op)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: TODO — probably no-op for us (yak …-c5ql)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
A browsable/categorized index is core PKM-tool territory — likely a *no-op* for
org-gtd. Confirm during the action-surface investigation.

** Delegate
*** DROP REC-DEL-04 — two-stage delegation state                        :may:delegate:
:PROPERTIES:
:REC_ID:      REC-DEL-04
:STATUS:      Not-implemented → dropped (feature); research yak filed
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: DROP the feature + research yak on delegation-UX impact
:DECIDED:     2026-07-15
:END:
The two stages already map onto existing types: plan-to-delegate = a next action
("ask Bob to take over X"); delegated = re-clarify as delegated once handed off.
A first-class two-stage state isn't necessary.

*Decision*: DROP the feature. But a real UX question was raised — does the
*one-step* delegation UX quietly encourage delegating *in the system* without
actually talking to the delegatee? Tracked as a research yak
=research-does-org-gtds-one-step-delegation-ux-...-del-04-1u47=. If that research
shows a problem, a conversation-nudge or two-stage design gets reconsidered then.

*** BUILD-V5 REC-DEL-03 — delegate a whole project as one Waiting-For   :should:delegate:
:PROPERTIES:
:REC_ID:      REC-DEL-03
:STATUS:      Not-implemented → BUILD (design-gated)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 — needs its own design for the snapshot/restore seam
:UX_DOC:      REC-DEL-03.md
:DECIDED:     2026-07-15
:END:
Today delegation is per-item (=org-gtd-delegate= sets =DELEGATED_TO= + check-in,
flips to =WAIT=). DEL-03 is handing off a *whole decomposed project* so it
leaves the active-projects list and shows as a single "Waiting for X to deliver
Y", subtasks collapsed.

*Decision*: BUILD-V5 (honors adjudicated #6), **design-gated**. The
projects↔delegate *snapshot/restore seam* needs its own design doc before
implementation — it touches the project state machine, the delegate model,
org-edna deps, archive/restore, stuck detection, and check-in behaviour.
Not a one-function add. (Same design-first class as [[*BUILD-V5 REC-PRJ-04 — subproject rollup (+ parallel/sequential)][REC-PRJ-04]] rollup.)

** Agendas
*** DEFER REC-AGE-01 — per-person / per-meeting "talk-to" lists         :should:agendas:
:PROPERTIES:
:REC_ID:      REC-AGE-01
:STATUS:      Partial → docs howto (no bundled feature)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs howto
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
GTD's Agendas list (things to *raise with* a person / at a meeting) is distinct
from delegation (the =who= filter is =DELEGATED_TO=-specific). But people do this
idiosyncratically — tags + a saved view, or a personal knowledge base
(org-roam/denote) to maintain per-person notes.

*Decision*: DEFER → docs howto covering approaches (a tag convention + a saved
view via the view builder; or leaning on a PKB). No bundled feature — consistent
with keeping contact/reference *resolution* to a future adapter layer. See also
[[*REC-AGE-02]] (aggregated per-person view) when we reach it.

** Tickler
*** BUILD-V5 REC-TIC-02 — tickler recurrence (+ lead-time to docs)     :should:tickler:
:PROPERTIES:
:REC_ID:      REC-TIC-02
:STATUS:      Partial → recurrence build; lead-time dropped
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (recurrence via org-habit-ng) + DROP lead-time to docs
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Source check (Sonnet, GTD Book 1 + MIAW): the tickler has *no* advance-warning
feature — lead time is achieved by filing the item under an earlier resurface
date you choose ("remind me two weeks ahead" = file it two weeks early). And
Allen only ever re-files recurring ticklers (birthdays, annual conference)
*by hand*. So my "lead-time notice" framing was an over-reach.

Decisions, split:
- *Lead-time* → *DROP → docs*. No feature. Docs note: for advance notice, either
  file the tickler for the earlier date, or use org-mode's =DEADLINE= property
  (with its warning cookie, e.g. =DEADLINE: <2026-08-01 -14d>=) which fills this
  purpose natively.
- *Recurrence* → *BUILD-V5* using **org-habit-ng** (library the maintainer
  wrote) as the recurrence engine for recurring ticklers — software rightly
  improving on the physical system's manual re-filing. Integration point for
  recurring reminders (annual/periodic ticklers).

** Checklists
*** DONE REC-CHK-01 — reusable checklists                          :checklist:
:PROPERTIES:
:REC_ID:      REC-CHK-01
:STATUS:      Implemented
:TYPE:        tool
:DISPOSITION: V-10 → implemented
:UX_DOC:      REC-CHK-01.md
:DECIDED:     2026-07-15
:END:
Landed on =develop= (former PR #294). By-name, per-workflow checklists
(=org-gtd-checklist.el=). Rejected corpus extras (CHECKLIST_KIND, edna RESET,
defcustom store, etc.) are recorded in the design doc §8 — do not resurrect.
** UI / Views
*** BUILD-V5 REC-UI-02 — proactive integrity flags (command-center counts) :should:ui:
:PROPERTIES:
:REC_ID:      REC-UI-02
:STATUS:      Partial → building count annotations
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (counts) + larger dashboard-rework aspiration
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Detection exists but is reactive (command center → "Stuck items..." / "Missed
items...", =org-gtd-command-center.el:67=). Nothing flags "you have 3 stuck
projects" until you look.

*Decision*: BUILD-V5 — annotate the command-center entries with *live counts*
("Stuck items (3)", "Missed items (2)"), maybe a one-line integrity summary at
the top. Non-intrusive (only when you're already in org-gtd) — no nag / no
mode-line badge / no startup warning (org-gtd stays non-preachy).

*Larger aspiration* (weigh at [[*REC-UI-01]]): rework the command center toward a
*dashboard-style navigation pane*. Reference:
https://github.com/aravindps/org-gtd (see
=screenshots/themes-card_files/Navigation Pane.png=). The count annotations are
the first step; the full dashboard rework is a bigger UX design decided together
with UI-01 (dashboard home view).

*** BUILD-V5 REC-UI-04 — consent-based migrate overdue calendar → next-action :should:ui:
:PROPERTIES:
:REC_ID:      REC-UI-04
:STATUS:      Not-implemented → BUILD on the generalized walk engine
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (rides the generalized item-walk foundation)
:UX_DOC:      REC-UI-04.md
:DECIDED:     2026-07-15
:END:
When a calendar appointment's date passes uncompleted, *offer* (with veto) to
migrate it into a next action — consent-only, no auto-action, no forced agenda.

*Decision*: BUILD-V5 as the first real consumer of the generalized item-walk
(see [[*BUILD-V5 (foundational) — generalize the item-walk engine][Foundations]]): walk the missed calendar items one at a time with a
"retype to next-action" action (reuses reactivate/retype infra in
=org-gtd-reactivate.el=). Gated on the walk-engine generalization.

*** BUILD-V5 REC-UI-05 — mark-done closure prompt ("what's next?")     :should:ui:
:PROPERTIES:
:REC_ID:      REC-UI-05
:STATUS:      Partial → building chain-end closure prompt
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: BUILD-V5 (also fixes the under-defined-project gap, PRJ-08 spillover)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Today =org-gtd-projects--edna-update-project-after-task-done= (=org-gtd-projects.el:1417=)
advances successors on task-done, but when the chain runs out the project *sits
silent* until a stuck-view catches it. That silent moment is the PRJ-08 concern:
all-tasks-done reads as complete, but the project may be merely *under-defined*.

*Decision*: BUILD-V5 — a *chain-end closure prompt*: when the last action
completes with nothing queued, ask *is this project complete?* (archive/close)
or *does it need more actions?* (under-defined → add next action(s) / send to
stuck). Hooks the existing task-done edna action. Delivers UI-05 *and* the
under-defined-project fix. Design note: only at genuine chain-end; respect a
"don't ask" path (non-naggy).



*** DONE NEW-VIEW-MANAGER — view builder & manager                 :ui:views:
:PROPERTIES:
:REC_ID:      NEW-VIEW-MANAGER
:STATUS:      Implemented
:TYPE:        tool
:UX_DOC:      NEW-VIEW-MANAGER.md
:DECIDED:     2026-07-15
:END:
Landed on =develop=. Transient builder for view specs (flat + multi-section),
persisted store, and a completing-read picker with section-badge annotations
feeding a select-then-act action menu.
** Cross-cutting
*** DEFER REC-X-14 — system available anywhere / mobile                 :should:cross:
:PROPERTIES:
:REC_ID:      REC-X-14
:STATUS:      Via-config → docs howto
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: DEFER → docs howto
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Nothing to build — org-gtd files are plain org; "available anywhere" is a
portability + ecosystem story, already true.

*Decision*: DEFER → docs howto — "Using org-gtd away from your computer": sync
the =org-gtd-directory= (e.g. Nextcloud), capture on mobile via an org app
(Orgzly/beorg) into one or two inboxes, process later in Emacs. Recommend
concrete tools; org-gtd builds no mobile client.

*** TODO REC-X-10 — unified, friendlier new-user experience            :should:cross:
:PROPERTIES:
:REC_ID:      REC-X-10
:STATUS:      Via-config → TODO (UX investment)
:STRENGTH:    should
:TYPE:        tool
:DISPOSITION: TODO — unified onboarding UX (yak …-vm2c)
:UX_DOC:      (none)
:DECIDED:     2026-07-15
:END:
Onboarding infra exists — =org-gtd-init-system= (first-time concierge) +
=org-gtd-setup-keywords-wizard=. But they're separate pieces. The maintainer
wants a focused push on a *unified, friendlier* new-user experience: cohere the
concierge + keywords wizard + a getting-started flow (and maybe a seeded setup
meta-project) into one welcoming onboarding.

*Decision*: TODO — a real UX investment area, tracked as yak
=unified-friendlier-new-user-experience-...-x-10-vm2c=. Migration-from-other-tools
stays out of scope (too varied). Scope/timing decided in its own design.

*** BUILD-V5 REC-X-04 — optional energy-level attribute              :may:cross:
:PROPERTIES:
:REC_ID:      REC-X-04
:STATUS:      Not-implemented → building with ENG-03
:STRENGTH:    may
:TYPE:        tool
:DISPOSITION: BUILD-V5 — resolved as part of REC-ENG-03
:UX_DOC:      REC-X-04.md
:DECIDED:     2026-07-15
:END:
Not a standalone item: the optional energy attribute *is* the energy criterion
built for REC-ENG-03 (see Engage). Off by default; =ENERGY= property + =energy=
DSL keyword.
